[pytest]
testpaths = tests
markers =
    regression: tests against real five-season data and known business facts
    integration: end-to-end tests spanning multiple pipeline layers
    smoke: fast tests that verify the pipeline can start and produce outputs
